// #include <stdio.h>
// #include <ctype.h>

// int main(int argc, char **argv, char **envp)
// {
//     int c;
//     while ((c = getchar()) != EOF)
//         putchar(c);
// }

#include <stdio.h>
#include <ctype.h>

int main(int argc, char **argv, char **envp)
{
    int c;
    while (c != EOF)
    {
        c = getchar();
        putchar(c);
    }
}